def merge( nums1, m, nums2, n) -> None:
        # length_of_loop = m if m > n else n
        # for i in range(length_of_loop):
        nums1 = sorted(nums1.extend(nums2))
        print(nums1)
        nums1 = sorted(nums1)
        print(nums1)
        